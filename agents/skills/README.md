# Agent skills

Place shared user-level skills in this directory using the following structure:

```text
skills/
└── <skill-name>/
    └── SKILL.md
```

The installation script links this directory to the user-level skill location used by supported agents.
